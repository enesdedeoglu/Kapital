{
  "name": "@kapital/sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "sim": "tsx src/cli/run-sim.ts",
    "sweep": "tsx src/cli/sweep.ts",
    "gate": "tsx src/cli/gate.ts"
  },
  "dependencies": {
    "@kapital/api": "workspace:*",
    "@kapital/db": "workspace:*",
    "@kapital/economy": "workspace:*",
    "@kapital/engine": "workspace:*",
    "@kapital/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
